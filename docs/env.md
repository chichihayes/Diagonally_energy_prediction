# docs/env.md â€” Diagonally Energy Prediction

## Required environment variables

```
MODEL_PATH=src/model/trained/model.joblib
API_HOST=0.0.0.0
API_PORT=8000
```

## Where to get each
- MODEL_PATH: path to the trained model file â€” generated by running scripts/run_training.py
- API_HOST: host to bind the FastAPI server (default 0.0.0.0 for all interfaces)
- API_PORT: port to run the API on (default 8000)
